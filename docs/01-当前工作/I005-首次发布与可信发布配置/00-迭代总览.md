# I005 首次发布与可信发布配置

状态：阻塞  
负责人：Codex  
优先级：P1  
更新时间：2026-09-06 15:05（Asia/Shanghai）

## 目标与边界

按用户“你来发布”的授权，首次公开发布 `@michengai/dsh-btw@0.1.0`，生成对应 GitHub Release，并配置 npm Trusted Publisher。不更改插件行为、不发布额外测试版本、不重启 DSH。

## 任务与验收

- [x] 确认 npm 目标版本尚不存在、账号已登录、Apache-2.0 与中英文 README 一致。
- [x] 两份 README 增加 npm 安装方式，首发版本号保持 `0.1.0`。
- [x] 重新构建并校验安装包，首次发布到 npm，核对线上版本与许可证。
- [x] 推送 `v0.1.0` 标签，验证 GitHub Release 标题、双语正文和安装包附件。
- [ ] 在 npm 中配置 `MichengAI/dsh-btw`、`publish.yml`、允许 `npm publish` 的 Trusted Publisher，随后启用仓库发布变量。
- [ ] 刷新发布文档、状态及待办，归档本迭代。

## 已完成与待处理

首次 npm 发布已完成，[线上包](https://www.npmjs.com/package/@michengai/dsh-btw) 为公开 `0.1.0`，许可证为 Apache-2.0。35 文件安装包检查通过，注册表 SHA1 为 `091c7d6d061b0b39bff9f738507ee990ddf19f97`，与本地发布包一致。

`v0.1.0` 标签指向 `6bab7a5`。[发布工作流 34017954665](https://github.com/MichengAI/dsh-btw/actions/runs/34017954665) 的 Node.js 22、24 检查及浏览器回归通过；[GitHub Release](https://github.com/MichengAI/dsh-btw/releases/tag/v0.1.0) 标题、中文在前的双语正文和 `.tgz` 附件均已核对。首次标签发布保持 `NPM_PUBLISH_ENABLED=false`，避免重复发布 npm。

Trusted Publisher 已填写 `MichengAI/dsh-btw`、`publish.yml`，Environment 留空，勾选直接 `npm publish`。待用户完成保存时的安全密钥验证，确认连接后再打开仓库变量；当前尚未启用。

阻塞：npm 保存配置要求本人安全验证，等待用户完成。关联提交：`6bab7a5`（首发准备与发布标签）。

阅读顺序：本总览 → [自动发布](../../05-工程交付/01-自动发布.md) → [中文 README](../../../README.zh-CN.md)。
