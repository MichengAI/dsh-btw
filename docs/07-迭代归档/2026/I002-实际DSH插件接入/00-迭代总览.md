# I002 实际 DSH 插件接入

状态：已完成
负责人：Codex  
优先级：P1  
更新时间：2026-09-06 12:26（Asia/Shanghai）

## 目标与边界

用户明确要求在实际 DSH 内使用，不接受模拟独立页面作为功能交付。把已有插件装入实际 Web profile，使用原聊天输入框及现有侧栏。无需将 preview 代码部署到 DSH。

## 任务与已完成

- [x] 确认正在运行的 DSH Codex Desktop 使用 `.dsh/profiles/web`，已装 `dsh-better-sidebar`。
- [x] 核对官方 `dsh plugin --profile web add` 自动安装及 bundle 装配机制，版本 0.1.2-rc.1。
- [x] README 优先说明真实 DSH 安装入口，明确模拟页面仅供开发测试。
- [x] 安装插件并核对宿主重载、装配健康状态。
- [x] 按用户“安装到本地，我自己测试”的要求交接实际使用验收。
- [x] 修复用户截图中的客户端命名空间注入报错与气泡宽度，重新构建并验证本地安装产物，见[修复记录](02-客户端注入与气泡宽度修复.md)。

## 阻塞与待处理

用户已明确要求安装到本地并自行测试。实际交互验收转入[项目待办](../../../00-交接入口/03-待办与阻塞.md)，不阻塞本次安装交付。

随后用户要求先卸载其他插件避免干扰。已完成启用项及依赖移除，见[测试环境隔离](01-测试环境隔离.md)；该记录描述当前测试配置，上述安装验收保留为执行证据。

实际 DSH 地址有认证；独立浏览器未继承桌面端认证，无法直接打开。未读取凭据或绕过认证。

## 验收

当前 `web` profile 新增 `@michengai/dsh-btw` 依赖及 bundle，`node_modules/@michengai/dsh-btw` 通过 Windows Junction 指向本仓库。沿用现有本地插件的 `link:` 约定；`pnpm-lock.yaml` 同步了对应 importer。

常规 tarball 安装在复核既有锁文件时，被 pnpm 11.25 的最小发布时间策略拦截，涉及 3 个既有插件。本次未调整策略、未重装被拦截的包，改为链接本次自主开发的代码；原有 10 个依赖版本均保持一致。

DSH 在 2026-09-06 12:07:14（Asia/Shanghai）自动重载，12:07:19 报告 `healthy`。从真实 profile 解析并导入插件成功，`apply` 为函数，依赖服务为 commands、subagents、tools。

实际 UI 与模型由用户测试，未宣称已经完成。功能只使用聊天输入框上方的气泡，不新增或改动侧栏。

## 恢复方式

安装前 package.json、pnpm-lock.yaml、pnpm-workspace.yaml 备份保存在项目 `artifacts/profile-backup-20260906-1205`，该目录已忽略版本控制。停用时从 profile bundle 移除本插件即可触发重载；完整卸载使用 README 的官方 remove 命令。若 pnpm 仍因既有包策略阻塞，应仅撤回本插件依赖、锁文件 importer 与 Junction，保留之后的其他配置改动，禁止递归删除 Junction 指向的源仓库。

关联提交：未提交。

阅读顺序：本总览 → [README](../../../../README.md) → [当前状态](../../../00-交接入口/02-当前状态.md)。
