# 测试环境隔离

更新时间：2026-09-06 13:32（Asia/Shanghai）

## 用户要求与结果

用户要求“把其他插件先卸载了，避免干扰”。在当前 Web profile 移除以下 10 个第三方插件的依赖和 bundle：

- @michengai/dsh-agency-agents
- @michengai/dsh-archive-manager
- @michengai/dsh-automation
- @michengai/dsh-codex-ui
- @michengai/dsh-im-connect
- @michengai/dsh-skills-manager
- dsh-better-sidebar
- dsh-context
- dsh-mcp-connector
- dshmarket

隔离完成时 dependencies 仅有本地链接的 @michengai/dsh-btw。bundle 仅有 @deepseek-ai/dsh-base、@deepseek-ai/dsh-web-app、dsh-desktop-bridge、@michengai/dsh-btw。未删除会话、凭据、插件业务数据或源码仓库。

2026-09-06 用户要求安装 Codex UI 测试样式兼容：恢复 `@michengai/dsh-codex-ui` 0.2.103 的本地链接依赖、bundle 和锁文件条目，复用已有 Junction 与构建产物。当前第三方启用项为 BTW 和 Codex UI，其余保持停用；实际样式兼容由用户测试。恢复前状态可通过移除这一个包的依赖和 bundle、锁文件条目还原，无需删除源码及偏好数据。

配置变更后桌面端于 13:31:43 自动重载，13:31:46 报告 healthy。Codex UI 服务端、客户端 JS 语法检查通过，安装链接与源码目录的客户端构建哈希一致；健康状态不代替样式兼容验收。

## 执行与验证

官方 `dsh plugin --profile web remove` 因 pnpm 11.25 检查既有锁文件而触发 minimumReleaseAge 错误。沿用桌面端 `src/plugin-seed.ts` 已有的卸载参数，对本次 remove 传入 `--config.minimumReleaseAge=0`，并使用 `--config.ignore-scripts=true --config.offline=true`。没有新增包下载或安装，也未修改持久策略；pnpm-workspace.yaml 与备份的 SHA256 一致。

依赖、bundle、锁文件已核对。7 个 registry 插件目录已移除；另外 3 个本地插件（agency-agents、automation、codex-ui）的 Junction 残留，删除操作被工具自动审批 `blocked by policy` 拦截，未改用其他方式重试。它们不在依赖或启用列表中，不参与当前 profile 装配。

DSH 于 2026-09-06 12:15:24（Asia/Shanghai）自动重载，12:15:27 报告 healthy。实际 BTW 界面和模型效果由用户自行测试。

## 恢复与持续性

卸载前的 package.json、pnpm-lock.yaml、pnpm-workspace.yaml、cordis.patch.yml 备份在项目 `artifacts/profile-backup-before-isolation-20260906`，已被 .gitignore 忽略。恢复需要按备份版本重新安装 7 个 registry 包并恢复 3 个本地链接及其依赖、bundle，不是只覆盖配置文件；恢复前应保留之后的新改动。

当前桌面端每次完整启动都会补装缺少的内置社区插件。卸载后的服务热重载不执行补装，因此当前环境保持隔离；若完整退出桌面端再启动，应重新核对插件清单。没有为此改动桌面端代码。
