# I003 宿主外观与国际化

状态：已完成  
负责人：Codex  
优先级：P1  
更新时间：2026-09-06 13:28（Asia/Shanghai）

## 目标与边界

修复用户截图中气泡与宿主风格不一致、浅色界面出现深色气泡、英文界面仍显示中文的问题。交付实际 DSH 插件，不增加独立使用页面，不修改侧栏、一次性子代理和工具禁用边界。

## 任务与已完成

- [x] 核对官方主题和 locale 服务实现，采用已安装同版本契约。
- [x] 移除系统 `prefers-color-scheme` 和不存在的 `dsw-alias-background-*` 变量，改用宿主语义令牌。
- [x] 去掉绿色左边框，统一中性边框、12px 圆角、字体、圆形工具按钮及正文留白。展开时向下收起，折叠时向上展开。
- [x] 注册 `michengai.btw` 中英文字典，slot 声明 locale 并使用宿主注入的 `t`；输入触发器使用实时绑定的翻译。
- [x] 覆盖按钮、状态、命令说明、输入校验、复制/关闭失败，以及服务器超时、取消、清理等提示；服务器沿用请求语言，不翻译模型和外部原始错误。
- [x] 重新构建，并核对本地 web profile 链接中的客户端、服务端文件与仓库哈希一致。
- [x] 完成 `btw-run`、`btw-close` 命令目录过滤代码。宿主0.1.2-rc.1没有hidden选项，使用可卸载兼容层，仅过滤这两个名称，不修改find/execute及宿主文件；真实菜单生效尚待后端重新加载，承接到项目待办。
- [x] 分类标题补齐国际化：中文“旁问”、英文“Side Questions”，`/btw`命令名不变。宿主将来源名作为分类标题且无独立标题字段，插件随语言变化注销旧来源并注册翻译后的名称，不改宿主字典。真实LocaleRuntime测试验证切换后只保留一个来源，桌面及移动语言交互回归2项通过。

## 验收

- 13:28 补充排查：用户截图及真实 Edge 菜单仍包含内部命令，此前仅直接调用 CommandRuntime，不能证明实际菜单已生效。现已补测两套已安装宿主的真实 Typert Gateway RPC，列表隐藏、内部执行、卸载恢复均通过。运行中的桌面及独立 Web 后端均早于修复构建启动，`patchReload: live` 创建的 HMR 使用 `root: []`，不监听插件 JS；文件哈希一致不能作为运行代码已更新的证据。未擅自重启，实际菜单验收见[项目待办](../../../00-交接入口/03-待办与阻塞.md)。

- 类型检查和构建通过，25 项单元及运行时测试通过；新增真实CommandRuntime用例覆盖内部命令隐藏、提交/关闭仍可执行、其他命令保留及卸载恢复。
- 浏览器18项中17项通过、1项不适用跳过；最终针对链接颜色修复补跑4项主题用例全部通过，其余13项此前已通过。
- 新增主题检查覆盖系统 light/dark 与宿主浅色/深色交叉组合，桌面1280×900、移动390×844；正文、辅助图标、链接对比度均不低于4.5:1。
- 使用真实官方 LocaleRuntime 检查注册、注入与命令提示切换；验证气泡可见文案即时切换、错误语言、模型原文保留。
- 浏览器主题用例无 pageerror；界面截图及设计复核见[视觉验收](01-视觉验收.md)。

客户端 SHA-256：`257D7174D6FCF50F8F200414951F7357AC48654655DCE8D10B08463AC836A425`。

服务端 SHA-256：`49E9FA2BA1FA7B92685A2DA90564DFE46F38CFAFF5629DCBC0F9180490A6909B`。

## 阻塞与待处理

没有阻塞本地代码交付的事项。未重启用户主任务；完整 DSH 界面和真实模型复测统一承接到[项目待办](../../../00-交接入口/03-待办与阻塞.md)。本次验证使用真实主题/语言模块与模拟会话，不将测试页称为完整宿主验收。

## 恢复方式

本次未迁移历史数据或改变 profile 启用项。需要停用时，沿用 [I002 的恢复方式](../I002-实际DSH插件接入/00-迭代总览.md)。`btw-run` 新增可选 locale，省略仍兼容旧请求；返回结构和关闭命令不变。

关联提交：未提交。

阅读顺序：本总览 → [视觉验收](01-视觉验收.md) → [README](../../../../README.md)。
