# 视觉验收

更新时间：2026-09-06 12:56（Asia/Shanghai）

## 比较依据与限制

源图：用户在该迭代提供的浅色界面截图，2504×1322像素；源界面的CSS视口和设备倍率未知。参考对象为其中原生DSH的浅色表面、边框、字号及输入区域。历史源图保留在本地，不随仓库发布。

实现截图位于本地忽略目录 `artifacts/btw-theme-{light,dark}-{desktop,mobile}.png`，桌面1280×900、移动390×844，CSS尺寸与图片像素一致，deviceScaleFactor=1。另有 `btw-english-desktop.png` 和 `btw-english-mobile.png`；这些文件可通过浏览器测试重新生成，不随仓库发布。

已将用户源图和实现截图放在同次图像检查输入中查看整体及气泡区域。源图处于回答中，实现主题截图处于回答完成；正文和外壳为模拟数据，因此只比较本次气泡的风格及主题适配，不声称全页、行高或内容逐像素复刻。等待态和语言切换另由浏览器交互断言验证。

## 五项检查

| 项目 | 结论 |
| --- | --- |
| 字体与层级 | 使用宿主字体，问题13px/500，正文13px/1.7，辅助状态13px；窄屏长文本换行，未发现重叠。 |
| 间距与布局 | 保持输入框对齐，移除绿色强调边；12px圆角与官方输入dock相邻面板规范一致。正文不再额外缩进到图标下方。 |
| 颜色与令牌 | 背景、文本、边框、代码块、错误均使用宿主变量；系统主题与宿主选择相反时仍正确。正文、辅助图标、链接达到4.5:1。 |
| 图像与图标 | 气泡没有需要生成的位图，沿用Lucide线性图标；按钮尺寸固定，未使用替代插画或手绘图标。 |
| 文案与内容 | 提示和按钮接入宿主中英文字典；回答内容及外部原始错误保持原文。服务器提示按请求时的界面语言固定。 |

## 修正过程

1. 原始P1：宿主浅色而气泡深色，前景对比不足；移除独立系统主题判断，改为宿主令牌。
2. 原始P2：绿色强调边与宿主中性组件不协调；改为均匀细边框和宿主文字色。
3. 原始P1：英文界面显示中文提示；接入locale服务并验证即时切换。
4. 验证发现图标颜色过渡与背景切换不同步；移除图标颜色过渡，重新测试通过。
5. 验证发现默认品牌蓝的小字号链接对比度约4.23:1；采用正文色加下划线，补跑4项主题用例通过并重新查看截图。

## 结果

final result: passed

该结果限本次气泡组件及已列出的主题/语言状态，没有发现待修复P0/P1/P2问题。完整真实DSH和真实模型验收仍由用户进行，不能用此结论替代。
