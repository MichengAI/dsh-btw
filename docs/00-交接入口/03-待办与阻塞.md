# 待办与阻塞

更新时间：2026-09-06 14:34（Asia/Shanghai）

| 事项 | 负责人 | 优先级 | 下一步与解除条件 |
| --- | --- | --- | --- |
| 启用 npm 自动发布 | 用户 | P2 | 在 npm 包设置中为 `MichengAI/dsh-btw` 的 `publish.yml` 配置 Trusted Publisher，再将仓库变量 `NPM_PUBLISH_ENABLED` 改为 `true`。当前关闭，不阻塞 GitHub Release 和安装包。步骤见[自动发布](../05-工程交付/01-自动发布.md)。 |
| 内部命令隐藏生效 | 用户、Codex | P1 | 文件已安装，两套真实 RPC 回归通过；目前运行中的后端仍未加载修复。等允许中断任务时重新加载实际后端，再确认菜单不含 `btw-run`、`btw-close`。仅浏览器刷新不能更新后端。 |
| 实际 DSH 使用验收 | 用户 | P1 | 等主任务结束后重新加载，在原输入框复测 `/btw`、关闭，以及浅深色和中英文切换；BTW 只保留旁问，上下键输入历史已移至 Codex UI，由该项目验收。真实模型行为及样式共存仍由用户反馈。 |

没有阻塞本地代码交付的外部依赖。
