# 待办与阻塞

更新时间：2026-09-06 15:05（Asia/Shanghai）

| 事项 | 负责人 | 优先级 | 下一步与解除条件 |
| --- | --- | --- | --- |
| 启用 npm 自动发布 | 用户、Codex | P2 | 首版已发布，Trusted Publisher 配置已填写；用户完成保存时的安全密钥验证后，由 Codex 核对连接并将 `NPM_PUBLISH_ENABLED` 改为 `true`。当前关闭，发布记录见 [I005](../01-当前工作/I005-首次发布与可信发布配置/00-迭代总览.md)。 |
| 内部命令隐藏生效 | 用户、Codex | P1 | 文件已安装，两套真实 RPC 回归通过；目前运行中的后端仍未加载修复。等允许中断任务时重新加载实际后端，再确认菜单不含 `btw-run`、`btw-close`。仅浏览器刷新不能更新后端。 |
| 实际 DSH 使用验收 | 用户 | P1 | 等主任务结束后重新加载，在原输入框复测 `/btw`、关闭，以及浅深色和中英文切换；BTW 只保留旁问，上下键输入历史已移至 Codex UI，由该项目验收。真实模型行为及样式共存仍由用户反馈。 |

没有阻塞本地代码交付的外部依赖。
