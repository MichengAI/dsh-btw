# 当前状态

更新时间：2026-09-06 14:31（Asia/Shanghai）

- 阶段：准备公开仓库和自动发布流程，活动迭代为 [I004](../01-当前工作/I004-仓库与自动发布/00-迭代总览.md)。实际 DSH 本地使用测试仍由用户进行。
- 当前验收依据：[I003 宿主外观与国际化](../07-迭代归档/2026/I003-宿主外观与国际化/00-迭代总览.md)；基础能力见 [I001](../07-迭代归档/2026/I001-一次性旁问气泡与输入历史/00-迭代总览.md)。
- 目标宿主：DSH 0.1.2-rc.1，Web 客户端。
- 边界：只提供一次性 fork 旁问、运行时禁用全部工具及独立气泡，不占侧边栏，不创建持续侧聊。上下键输入历史已迁至 Codex UI，BTW 不再包含采集、持久化或键盘绑定代码。
- 迁出验证：21 项单元/运行时测试、构建通过；桌面/移动浏览器 17 项通过、1 项不适用跳过，包含 BTW 不拦截上下键、不写历史存储的回归。历史导航及旧数据迁移由 Codex UI 独立验收。
- 待验证：真实模型旁问及 Codex UI 样式共存。详见[待办](03-待办与阻塞.md)。
- `web` profile 保留 BTW 本地开发链接；按用户最新要求移除 Codex UI 加载和依赖配置，以测试原生界面。其安装目录链接仍保留，但不再配置加载。
- 公开 README 已加入三张实际界面截图，截图仅展示界面，不代替全部行为验收。GitHub 仓库和 Actions 验证进行中，尚未发布 npm 版本。
- 桌面端完整退出后再启动会补装内置插件；当前热重载不会补装。3 个未启用的目录链接因工具策略未删除。
- 已接入宿主浅深色令牌、locale 服务及中英文文案，目录过滤代码和本地链接产物已更新；两套宿主真实 RPC 回归通过。此前运行后端未加载隐藏修复，用户随后提供的菜单截图显示“旁问”分类，但不足以验证整个命令目录，完整隐藏验收仍见[待办](03-待办与阻塞.md)。后端 HMR 的 `root: []` 不监听插件 JS，须重新加载实际后端，不能仅刷新浏览器。
